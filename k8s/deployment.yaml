apiVersion: apps/v1
kind: Deployment
metadata:
  name: "adriaanagent"
  namespace: "agents-devteam"
  labels:
    app: "adriaanagent"
    team: "devteam"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "adriaanagent"
  template:
    metadata:
      labels:
        app: "adriaanagent"
        team: "devteam"
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: "sa-adriaanagent"
      containers:
        - name: "adriaanagent"
          image: "acrshoppoc.azurecr.io/agents/adriaanagent:latest"
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: "config-adriaanagent"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
